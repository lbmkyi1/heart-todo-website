const $=id=>document.getElementById(id),dateEl=$('date'),input=$('input'),addBtn=$('add'),list=$('list'),leftEl=$('left'),clearBtn=$('clear'),progressFill=$('progressFill'),progressText=$('progressText'),music=$('music');let todos=JSON.parse(localStorage.getItem('todos'))||[],filter='active';const icons=['🌸','✨','🎀','🌷','💫','🍀','🌺','🌼','⭐','🔆'];dateEl.textContent=new Date().toLocaleDateString('en-US',{weekday:'long',month:'short',day:'numeric'});function save(){localStorage.setItem('todos',JSON.stringify(todos))}function getIcon(){return icons[Math.floor(Math.random()*icons.length)]}function updateProgress(){const total=todos.length,completed=todos.filter(t=>t.done).length,percent=total?Math.round((completed/total)*100):0;progressFill.style.width=percent+'%';progressText.textContent=percent+'% Complete ('+completed+'/'+total+')'}function render(){let items=todos;'active'===filter?items=todos.filter(t=>!t.done):items=todos.filter(t=>t.done);list.innerHTML=items.map(t=>`<li class="${t.done?'done':''}"><div class="task-icon ${t.done?'done':''}">${t.icon||getIcon()}</div><div class="task-content"><div class="task-text">${t.text.replace(/</g,'&lt;')}</div><div class="task-time">${new Date(t.id).toLocaleTimeString('en-US',{hour:'2-digit',minute:'2-digit'})}</div></div><div class="task-btns"><button class="task-btn ${t.done?'uncheck':'check'}" onclick="toggle(${t.id})"><i class="fas fa-${t.done?'undo':'check'}"></i></button><button class="task-btn del" onclick="del(${t.id})"><i class="fas fa-trash"></i></button></div></li>`).join('');leftEl.textContent=todos.filter(t=>!t.done).length;updateProgress()}function add(){const text=input.value.trim();if(!text)return;todos.unshift({id:Date.now(),text:text,done:!1,icon:getIcon()});save();input.value='';render()}function toggle(id){const t=todos.find(x=>x.id===id);t&&(t.done=!t.done,save(),render())}function del(id){todos=todos.filter(x=>x.id!==id);save();render()}addBtn.addEventListener('click',add);input.addEventListener('keydown',e=>{if('Enter'===e.key&&!e.shiftKey){e.preventDefault();add()}});clearBtn.addEventListener('click',()=>{todos=todos.filter(t=>!t.done);save();render()});document.querySelectorAll('.filter-btn').forEach(btn=>{btn.addEventListener('click',()=>{document.querySelectorAll('.filter-btn').forEach(b=>b.classList.remove('active'));btn.classList.add('active');filter=btn.dataset.filter;render()})});window.toggle=toggle;window.del=del;let playing=!1;document.body.addEventListener('click',()=>{if(!playing){try{let A=window.AudioContext||window.webkitAudioContext;if(A){let a=new A(),s=a.createMediaElementSource(music),g=a.createGain();g.gain.value=.1;s.connect(g);g.connect(a.destination);a.resume()}}catch(e){music.volume=.035}music.play().catch(()=>{});playing=!0}},{once:!0});render();
